<%= form_with(model: mineracao, local: true) do |form| %>
  <% if mineracao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mineracao.errors.count, "error") %> prohibited this mineracao from being saved:</h2>

      <ul>
      <% mineracao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :acronimo %>
    <%= form.text_field :acronimo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
